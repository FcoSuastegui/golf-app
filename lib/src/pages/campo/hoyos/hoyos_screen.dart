import 'package:flutter/material.dart';

class HoyoScreen extends StatelessWidget {
  const HoyoScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Center")
      ),
    );
  }
}