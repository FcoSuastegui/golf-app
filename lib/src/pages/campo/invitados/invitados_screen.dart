import 'package:flutter/material.dart';

class InvitadoScreen extends StatelessWidget {
  const InvitadoScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Center")
      ),
    );
  }
}