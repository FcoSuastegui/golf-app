import 'package:flutter/material.dart';

class TeeTimePage extends StatelessWidget {
  const TeeTimePage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Center")
      ),
    );
  }
}