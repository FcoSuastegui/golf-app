import 'package:flutter/material.dart';

class ReservacionesPage extends StatelessWidget {
  const ReservacionesPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Center")
      ),
    );
  }
}