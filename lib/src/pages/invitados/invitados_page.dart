import 'package:flutter/material.dart';

class InvitadosPage extends StatelessWidget {
  const InvitadosPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Center")
      ),
    );
  }
}